package frc.robot.lib;

/**
 * A class for filtering the contours of vision targets
 * posted to network tables by a grip pipeline running
 * on the limelight. For now only supports
 */
public class ContourFiltering {

  

}